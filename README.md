# 启航AI工作台 —— 个人 AI 助手

> 一款以**数据流为核心**的个人 AI 助手，强调"本地优先、模型自由、数据流转起来"——你的笔记、待办、工作数据都留在本机，AI 融入数据的采集、检索、执行与复盘全流程，而不只是一个聊天窗口。

启航AI工作台是一款桌面应用，把 **本地笔记知识库**、**结构化数据**、**待办提醒**、**飞书推送** 和 **AI 智能体** 整合成一套完整的数据流。所有数据都保存在你的电脑上，AI 模型可以自由选择（支持 DeepSeek、OpenAI 兼容接口，也可以使用本地 Ollama 完全离线运行）。

---

## ✨ 核心卖点：一条完整的自动化链路

```
采集 → 分析 → 建议 → 执行 → 推送
```

1. **采集**：Markdown 笔记随手记，JSON 数据批量导入，自动索引进本地知识库。
2. **分析**：AI 基于你的笔记和数据回答提问、查数据集、读文件，而不是凭空瞎编。
3. **建议**：AI 不只是回答问题，还给出可执行的建议——该做的事、该记的档、该跟进的事项。
4. **执行**：对话中产生的任务、记录直接落成待办和数据集，AI 还能读写笔记文件、操作本地数据。
5. **推送**：定时提醒、每日 AI 日报自动汇总，通过飞书推送到你的群和联系人。

> 全程数据留在本机，模型可自由选择，形成"采集 → 分析 → 建议 → 执行 → 推送"的完整闭环。

## 核心功能

| 功能 | 说明 |
|------|------|
| 💬 **AI 对话** | 与 AI 对话，支持图片粘贴/上传、模型切换、流式输出和工具调用 |
| 💻 **编程** | 代码项目工作台，管理项目文件树，基于项目的 AI 编程对话 |
| 📚 **知识库** | 浏览和管理本地 Markdown 笔记文件，支持关键词搜索 |
| 📊 **数据集** | 管理结构化数据（客户、项目、Bug 等），支持 JSON 导入导出 |
| ✅ **任务与提醒** | 待办看板、定时提醒，支持桌面通知和飞书推送 |
| 🔗 **飞书集成** | Webhook 推送消息；配置 Bot 后可在飞书里直接和 AI 对话 |
| 📋 **自动日报** | 每天定时汇总你的笔记、任务、对话，生成 AI 日报并推送 |
| 🧰 **工具箱** | 通用识别、识题、试卷识别、数据采集/加工等工具 |

## 快速开始

应用启动后，需要先完成以下配置才能正常使用：

### 1. 安装 pi agent（AI 对话引擎）

```bash
npm install -g @earendil-works/pi-coding-agent
```

安装后运行 `pi` 命令，按照提示完成登录和模型配置（推荐 DeepSeek 或 Ollama 模型）。

### 2. 设置笔记库目录

进入应用「设置 → 笔记库设置」，选择一个包含 Markdown 文件的本地文件夹作为笔记库。

### 3. 开始使用

完成以上配置后，即可在「对话」页开始与 AI 对话，在「知识库」浏览笔记文件，在「数据」页管理数据集。

## 使用指南

| 页面 | 怎么用 |
|------|--------|
| **对话** | 与 AI 对话，支持图片粘贴/上传、模型切换；pi agent 驱动 |
| **编程** | 管理代码项目，基于项目文件的 AI 编程对话 |
| **知识库** | 文件浏览 + 概览看板（日报、待办、提醒） |
| **数据** | 创建数据集，自定义 Schema，JSON/URL 批量导入 |
| **任务** | 管理待办和定时提醒；设置提醒时间后到点自动通知（桌面/飞书） |
| **工具箱** | 通用识别、识题、试卷识别等辅助工具 |
| **设置** | 笔记库、嵌入模型、飞书、定时任务、日报等配置 |
| **日志** | 操作日志查看，支持级别过滤和自动刷新 |

## 配置指南

### 1. LLM 对话模型（必配）

通过终端运行 `pi` 命令配置模型，支持：
- **DeepSeek**：填 API Key 选择模型
- **OpenAI 兼容接口**：任何 OpenAI 兼容的服务商
- **Ollama（本地/离线）**：填服务地址和模型名

配置后应用自动识别，对话页也可临时切换模型。

### 2. 飞书集成

- **Webhook 推送**：粘贴飞书群机器人 Webhook 地址，AI 日报、提醒即可推送到群
- **Bot 对话**：在飞书开放平台创建应用，填 `App ID` / `App Secret`，启动后可在飞书私聊/群聊中直接使用 AI

### 3. 定时任务与日报

「设置 → 定时任务」中可配置：

- **自动日报**：每天定时汇总笔记、任务、对话生成日报（可自定义提示词模板，推送到飞书）
- 支持定时索引等任务

## 数据与隐私

- 所有数据保存在本机数据库（`~/.qihang-work-ai/qihang-work-ai.db`）与你的笔记文件夹中。
- 对话内容默认会发送给你配置的模型服务商（DeepSeek 等）用于生成回复。
- 如需**完全离线**：使用 Ollama 作为对话模型与嵌入模型即可（除软件自动更新外无任何网络请求）。
- 建议定期备份数据库文件和笔记目录；后续版本将提供一键备份/恢复。

## 常见问题（FAQ）

**Q：提示"模型连接失败"？**
A：运行 `pi` 检查模型配置，Ollama 需先 `ollama pull <模型名>` 并确保服务已启动。

**Q：可以完全离线使用吗？**
A：可以。对话模型和嵌入模型都配置为 Ollama 本地模型即可。

**Q：数据会丢失吗？**
A：数据保存在本机数据库，正常退出不丢失；建议定期备份（见「数据与隐私」）。

---

## 开发与贡献

技术架构、改造方案与演进路线见 [`docs/改造方案.md`](docs/改造方案.md)；构建安装包使用 `npm run electron:build`。